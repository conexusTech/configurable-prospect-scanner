"""Scan phases this platform adds around the vendored engine.

The engine implements discovery and scoring. These are the PRD's other phases,
written here rather than in `av_lead_scanner.py` so the vendored file stays
byte-identical to upstream and re-vendoring stays a fast-forward (UPSTREAM.md).
"""
